<% @reports.each do |report| %>

    SCOUTING REPORT NUMBER: <%= report.id %>
    PLAYER NAME: <%= report.player_name %>
    BATS: <%= report.bats%>
    THROWS: <%= report.throws%>
    USERNAME:
   
<% end %>